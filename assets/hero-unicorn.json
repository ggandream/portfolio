{"history":[{"breakpoints":[],"visible":true,"aspectRatio":1,"userDownsample":1,"layerType":"effect","type":"gradient","usesPingPong":false,"texture":false,"animating":false,"mouseMomentum":0,"mouseSpring":0,"isMask":0,"description":"","compiledFragmentShaders":["#version 300 es\n// Copyright (c) Unicorn Studio. Licensed under the Unicorn Studio Commercial License.\n// Unauthorized copying, redistribution, or use in competing products is prohibited.\nprecision highp float;in vec2 vTextureCoord;uniform vec2 uMousePos;const float PI = 3.14159265359;vec2 rotate(vec2 coord, float angle) {\nfloat s = sin(angle);\nfloat c = cos(angle);\nreturn vec2(\ncoord.x * c - coord.y * s,\ncoord.x * s + coord.y * c\n);\n}out vec4 fragColor;vec3 getBgColor(vec2 uv) {return vec3(0, 0, 0);\n}void main() {vec2 uv = vTextureCoord;\nvec2 pos = vec2(0.5, 0.5) + mix(vec2(0), (uMousePos-0.5), 0.0000);\nuv -= pos;\nuv /= max(0.5000*2., 1e-5);\nuv = rotate(uv, (0.0000 - 0.5) * 2. * PI);\nvec4 color = vec4(getBgColor(uv), 1.0) * 1.0000;\nfragColor = color;\n}"],"compiledVertexShaders":["#version 300 es\n// Copyright (c) Unicorn Studio. Licensed under the Unicorn Studio Commercial License.\n// Unauthorized copying, redistribution, or use in competing products is prohibited.\nprecision mediump float;in vec3 aVertexPosition;\nin vec2 aTextureCoord;uniform mat4 uMVMatrix;\nuniform mat4 uPMatrix;out vec2 vTextureCoord;\nout vec3 vVertexPosition;void main() {\ngl_Position = uPMatrix * uMVMatrix * vec4(aVertexPosition, 1.0);\nvTextureCoord = aTextureCoord;\n}"],"speed":0.25,"trackMouse":0,"trackAxes":"xy","data":{"downSample":0.5,"depth":false,"uniforms":{},"isBackground":true},"id":"426a8c42-7834-42e4-995b-58f9982d845a","publicId":"gradient"},{"breakpoints":[],"visible":true,"aspectRatio":1,"userDownsample":1,"layerType":"effect","type":"custom","usesPingPong":false,"texture":false,"textures":[],"animating":false,"mouseMomentum":0,"mouseSpring":0,"isMask":0,"description":"A nostalgic sky effect featuring a soft blue horizon gradient, billowy cumulus cloud banks along the bottom, and soaring seagull silhouettes.","compiledFragmentShaders":["#version 300 es\n// Copyright (c) Unicorn Studio. Licensed under the Unicorn Studio Commercial License.\n// Unauthorized copying, redistribution, or use in competing products is prohibited.\nprecision highp float;in vec2 vTextureCoord;uniform sampler2D uTexture;\nuniform vec2 uResolution;\nuniform float uTime;out vec4 fragColor;float hash(vec2 p) {\np = fract(p * vec2(123.34, 456.21));\np += dot(p, p + 45.32);\nreturn fract(p.x * p.y);\n}float noise(vec2 p) {\nvec2 i = floor(p);\nvec2 f = fract(p);\nf = f * f * (3.0 - 2.0 * f);\nfloat a = hash(i);\nfloat b = hash(i + vec2(1.0, 0.0));\nfloat c = hash(i + vec2(0.0, 1.0));\nfloat d = hash(i + vec2(1.0, 1.0));\nreturn mix(mix(a, b, f.x), mix(c, d, f.x), f.y);\n}float fbm(vec2 p) {\nfloat v = 0.0;\nfloat a = 0.5;\nmat2 rot = mat2(0.8, 0.6, -0.6, 0.8);\nfor (int i = 0; i < 4; i++) {\nv += a * noise(p);\np = rot * p * 2.02 + vec2(1.7, 9.2);\na *= 0.5;\n}\nreturn v;\n}float drawSeagull(vec2 uv, vec2 center, float scale, float t, float id) {\nvec2 p = uv - center;\np.x *= uResolution.x / uResolution.y;\np /= scale;\nfloat tilt = sin(t * 0.7 + id * 2.3) * 0.15;\nfloat c = cos(tilt), s = sin(tilt);\np = mat2(c, -s, s, c) * p;if (abs(p.x) > 0.55 || abs(p.y) > 0.6) return 0.0;float x = abs(p.x);\nfloat flap = sin(t * 2.2 + id * 4.1) * 0.35;\nfloat wingShape = sin(x * 3.14159) * (0.28 + flap * 0.28) - x * 0.12;\nwingShape -= pow(x, 2.2) * 0.18;\nfloat body = exp(-x * 24.0) * 0.065;\nfloat targetY = wingShape + body;float thick = (0.028 + 0.032 * sin(x * 3.14159)) * smoothstep(0.5, 0.04, x);\nfloat dist = abs(p.y - targetY);float mask = smoothstep(thick + 0.01, thick - 0.01, dist) * smoothstep(0.52, 0.46, x);\nreturn mask;\n}void main() {\nvec2 uv = vTextureCoord;\nfloat t = uTime * 0.025;\nvec2 posOffset = (vec2(0.5, 0.5) - vec2(0.5)) * 0.4;\nfloat adjustedY = uv.y - posOffset.y;\nvec3 sky = mix(vec3(0.7, 0.81, 0.9), vec3(0.32, 0.54, 0.82), clamp(adjustedY, 0.0, 1.0));\nfloat horizonHaze = exp(-pow(adjustedY - 0.25, 2.0) * 12.0) * 0.15;\nsky += vec3(0.08, 0.08, 0.06) * horizonHaze;\nfloat baseH = 0.4500 * 0.45 + posOffset.y * 0.2;\nvec2 cloudUv1 = vec2(uv.x * 2.2 + t * 0.02, uv.y * 1.8);\nfloat n1 = fbm(cloudUv1 * 2.5 + vec2(3.1, 1.2));\nfloat backCloudMask = smoothstep(0.0, 0.2, baseH + 0.08 + (n1 - 0.48) * 0.35 - uv.y);\nvec2 cloudUv2 = vec2(uv.x * 1.8 + t * 0.04 + 1.5, uv.y * 1.5);\nfloat n2 = fbm(cloudUv2 * 3.0);\nfloat frontCloudMask = smoothstep(0.0, 0.18, baseH + (n2 - 0.45) * 0.4 - uv.y);\nvec3 backCloudCol = mix(sky, vec3(0.96, 0.96, 0.97) * 0.82, 0.75);\nvec3 frontCloudCol = mix(vec3(0.96, 0.96, 0.97) * 0.85, vec3(0.96, 0.96, 0.97), clamp((uv.y - (baseH - 0.2)) * 3.0, 0.0, 1.0));\nfrontCloudCol += vec3(0.06) * smoothstep(0.1, 0.3, n2);vec3 sceneColor = mix(sky, backCloudCol, backCloudMask * 0.7);\nsceneColor = mix(sceneColor, frontCloudCol, frontCloudMask);\nfloat birdsAlpha = 0.0;\nvec3 birdCol = vec3(0.98);for (int i = 0; i < 6; i++) {\nfloat id = float(i);\nfloat speedFactor = 0.02 + sin(id * 1.3) * 0.005;\nfloat birdX = fract(id * 0.18 + 0.1 + t * speedFactor + posOffset.x * 0.3);\nbirdX = birdX * 1.4 - 0.2;\nfloat birdY = 0.52 + sin(id * 2.1 + id) * 0.2 + sin(t * 0.3 + id) * 0.03 + posOffset.y;\nfloat scale = (0.03 + sin(id * 3.7) * 0.01) * (0.5000 / 0.5);\nfloat bMask = drawSeagull(uv, vec2(birdX, birdY), scale, uTime * 0.03, id);\nbirdsAlpha = max(birdsAlpha, bMask);\n}sceneColor = mix(sceneColor, birdCol, birdsAlpha);\nfloat grain = (hash(uv * uResolution.xy + fract(uTime * 0.05)) - 0.5) * 0.025;\nsceneColor += vec3(grain);\nvec4 bg = texture(uTexture, uv);\nvec3 finalRgb;\nfinalRgb = sceneColor;fragColor = vec4(mix(bg.rgb, finalRgb, 1.0), max(bg.a, 1.0));\n}"],"compiledVertexShaders":["#version 300 es\n// Copyright (c) Unicorn Studio. Licensed under the Unicorn Studio Commercial License.\n// Unauthorized copying, redistribution, or use in competing products is prohibited.\nprecision highp float;in vec3 aVertexPosition;\nin vec2 aTextureCoord;out vec2 vTextureCoord;\nout vec3 vVertexPosition;void main() {\ngl_Position = vec4(aVertexPosition, 1.0);\nvTextureCoord = (vec4(aTextureCoord, 0.0, 1.0)).xy;\n}"],"customPasses":[{"id":"sky-birds-pass","label":"1989 Sky & Seagulls","downSample":null,"includeBg":false,"needsPreviousFrame":null,"pingPong":false,"targetSize":null,"widthSegments":null,"heightSegments":null,"independentGeometry":null,"blend":null,"texturesOptions":null}],"trackMouse":0,"speed":0.5,"trackAxes":"xy","data":{"depth":false,"uniforms":{},"isBackground":false,"passes":[]},"id":"aa0f3c12-646d-4716-92c3-65ee2ca00f2b","publicId":"custom"},{"breakpoints":[],"visible":true,"aspectRatio":1,"userDownsample":1,"layerType":"effect","type":"stars","usesPingPong":false,"texture":false,"animating":true,"mouseMomentum":0,"mouseSpring":0,"isMask":0,"description":"","compiledFragmentShaders":["#version 300 es\n// Copyright (c) Unicorn Studio. Licensed under the Unicorn Studio Commercial License.\n// Unauthorized copying, redistribution, or use in competing products is prohibited.\nprecision highp float;in vec2 vTextureCoord;uniform sampler2D uTexture;\nuniform float uTime;uniform vec2 uMousePos;\nuniform vec2 uResolution;vec4 applyLayerMix(vec4 color, vec4 bg, float amount) {\ncolor.rgb = mix(bg.rgb, color.rgb, amount);\ncolor.a = max(bg.a, amount);\nreturn color;\n}out vec4 fragColor;const float PI = 3.14159265359;float luma(vec4 color) {\nreturn 0.299 * color.r + 0.587 * color.g + 0.114 * color.b;\n}vec2 hash(vec2 p) {\np = vec2(dot(p, vec2(127.1, 311.7)), dot(p, vec2(269.5, 183.3)));\nreturn fract(sin(p) * 18.5453);\n}mat2 rot(float a) {\nreturn mat2(cos(a),-sin(a),sin(a),cos(a));\n}vec3 hue(float h, float angle) {\nreturn vec3(0.5) + 0.5 * cos(h + 2.0 * PI * angle + vec3(0, 2, 4));\n}vec4 getStarLayer(vec2 baseUV, float scaleMult, vec2 offset) {\nvec2 scaleRatio = vec2(1080) * vec2(uResolution.x/uResolution.y, 1);\nvec2 pos = vec2(0.5, 0.5) + mix(vec2(0), (uMousePos-0.5), 0.0100);\nvec2 uv = (baseUV - pos) * scaleRatio * 1.1960 * scaleMult * 0.01 * rot(0.1485 * PI * 2.) + offset;\nfloat time = (uTime * 0.0005);vec2 i_uv = floor(uv);\nvec2 f_uv = fract(uv);\nvec2 point = vec2(0);vec3 d = vec3(1e10);\nvec2 closestPoint;for (int y = -1; y <= 1; y++) {\nfor (int x = -1; x <= 1; x++) {\nvec2 tile_offset = vec2(float(x), float(y));\nvec2 o = hash(i_uv + tile_offset + vec2(time * 0.05));\ntile_offset += o - f_uv;float dist = dot(tile_offset, tile_offset);\nif (dist < d.x) {\nd.y = d.x;\nd.x = dist;\nclosestPoint = tile_offset;\npoint = o;\n} else if (dist < d.y) {\nd.y = dist;\n}\n}\n}d = sqrt(d);\nfloat r = sqrt(d.x);vec2 toCenter = closestPoint;\nvec2 toCenterRot45 = toCenter * rot(-PI/4.);vec2 closestPointOriginal = closestPoint * rot(-abs(0.1485) * PI * 2.) / (1.1960 * scaleMult * 0.01) / scaleRatio + baseUV;vec4 closestPointCol = texture(uTexture, closestPointOriginal);\nfloat closestPointR = luma(closestPointCol);\nfloat mixThresh = mix(1., closestPointR, 0.1000);\nfloat stepThresh = step(0.1000, closestPointR);\nclosestPointR = mixThresh;float crossShape;\nfloat crossShape2;\ncrossShape = min(abs(toCenter.x), abs(toCenter.y));\ncrossShape2 = min(abs(toCenterRot45.x), abs(toCenterRot45.y));\nfloat proximityFactor = sqrt(d.y) - sqrt(d.x);\nfloat radialGradient = (1.0 - length(toCenter)) * closestPointR * (0.0600 * 1.5);\nfloat flare = 1.0 - smoothstep(-0.5, 1.0, 0.0800);\ncrossShape = 1.0 - smoothstep(-0.04, 0.04 * (proximityFactor - flare) * closestPointR * closestPointR, crossShape);vec3 cross = mix(vec3(crossShape), vec3(crossShape) * hue(closestPointR, proximityFactor*5.), 0.1600);\nvec3 bloom = vec3(smoothstep(0., 4., radialGradient * proximityFactor));\nvec3 rgb = mix(vec3(1, 1, 1), mix(vec3(1, 1, 1), closestPointCol.rgb, 0.5), 0.1000);\nreturn vec4(rgb * (cross + bloom) * 10., (cross.r + bloom.r) * 10. * rgb.r);\n}void main() {\nvec4 stars = getStarLayer(vTextureCoord, 1.0, vec2(0.0));vec4 bg = texture(uTexture, vTextureCoord);\nvec4 color = applyLayerMix(bg + stars, bg, max(bg.a, stars.a));\nfragColor = color;}"],"compiledVertexShaders":["#version 300 es\n// Copyright (c) Unicorn Studio. Licensed under the Unicorn Studio Commercial License.\n// Unauthorized copying, redistribution, or use in competing products is prohibited.\nprecision mediump float;in vec3 aVertexPosition;\nin vec2 aTextureCoord;uniform mat4 uMVMatrix;\nuniform mat4 uPMatrix;\nuniform mat4 uTextureMatrix;out vec2 vTextureCoord;\nout vec3 vVertexPosition;void main() {\ngl_Position = uPMatrix * uMVMatrix * vec4(aVertexPosition, 1.0);\nvTextureCoord = (uTextureMatrix * vec4(aTextureCoord, 0.0, 1.0)).xy;\n}"],"speed":0.01,"trackMouse":0.01,"trackAxes":"xy","data":{"depth":false,"uniforms":{},"isBackground":false},"id":"80ed3f08-49d6-4092-809f-ffbc91fc643b","publicId":"sparkle"}],"options":{"name":"Shadows (Remix)","fps":60,"dpi":1.5,"scale":1,"includeLogo":false,"isProduction":false,"flatten":false},"version":"2.2.8","id":"Lqbqoaa5HsTNjhAcsP2I"}